# Normalize line endings: text files stored as LF in the repo.
* text=auto eol=lf

# Binary assets — never touch line endings or try to diff/merge these.
*.glb binary
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.webp binary
